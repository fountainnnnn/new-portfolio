# Decidr Frontend

Next.js, TypeScript, Tailwind CSS, shadcn/ui, and Plotly frontend for Decidr - the dashboard builder that turns CSV data into decisions.

```powershell
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the FastAPI backend is not running on `http://localhost:8000`.
